import { PageOverheadDirective } from './page-overhead.directive';

describe('PageOverheadDirective', () => {
  it('should create an instance', () => {
    const directive = new PageOverheadDirective();
    expect(directive).toBeTruthy();
  });
});
